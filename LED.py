import pigpio

pin = 17
intensity = 255

#pi = pigpio.pi()


def turnOnLEDS():
    print("Doesn't actually work yet")
    #pi.set_PWM_dutycycle(pin, intensity)
    s= 1+1


def turnOffLEDS():
    #pi.set_PWM_dutycycle(pin, 0)
    s=1+1
